<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>PagedAttention on 技术博客</title><link>https://mingyu110.github.io/tech-blog-/tags/pagedattention/</link><description>Recent content in PagedAttention on 技术博客</description><generator>Hugo -- gohugo.io</generator><language>zh-cn</language><lastBuildDate>Sun, 03 Aug 2025 00:00:00 +0000</lastBuildDate><atom:link href="https://mingyu110.github.io/tech-blog-/tags/pagedattention/index.xml" rel="self" type="application/rss+xml"/><item><title>vLLM革命性LLM推理加速引擎技术深度解析</title><link>https://mingyu110.github.io/tech-blog-/posts/vllm-inference-acceleration-deep-dive/</link><pubDate>Sun, 03 Aug 2025 00:00:00 +0000</pubDate><guid>https://mingyu110.github.io/tech-blog-/posts/vllm-inference-acceleration-deep-dive/</guid><description>vLLM：革命性LLM推理加速引擎技术解析 摘要 vLLM是一个革命性的、开源的大语言模型（LLM）推理加速引擎。它通过创新的内存管理和请求调度技术，旨在显著提升LLM服务的吞吐量（Throughput）和计算效率，从而降低服务成本。
1. vLLM要解决的核心问题：KV Cache的内存瓶颈 问题根源: LLM在推理（生成文本）时，会产生一个巨大的键值缓存（KV Cache），用于存储注意力机制的中间结果。这个KV Cache非常消耗显存，并且其大小随生成长度动态变化，难以高效管理。 传统方法的弊端: 巨大的内存浪费: 传统框架（如HuggingFace Transformers的标准实现）通常会为每个请求预留一块能够容纳其最大可能长度的连续内存块来存储KV Cache。这导致大量显存被预留但并未实际使用。 严重的内存碎片: 当长短不一的请求完成后，会释放出大小不一的内存空洞。这些不连续的、零碎的内存块（碎片）很难被新的请求有效利用。 低吞吐量: 由于内存效率低下，一块GPU上能同时处理的并发请求数量非常有限。 2. vLLM的核心创新：PagedAttention 技术类比: PagedAttention是vLLM的灵魂所在。它借鉴了现代计算机操作系统中**虚拟内存（Virtual Memory）和分页（Paging）**的核心思想来管理KV Cache。 工作原理: 它不再要求KV Cache在物理显存中是连续的，而是将其分割成许多个固定大小的**“块”（Blocks）**。 这些块可以存储在物理显存的任意位置。 系统为每个请求维护一个**“块表”（Block Table）**，这个表的作用类似于操作系统中的页表，它记录了逻辑上连续的KV序列实际上存储在哪些物理块中。 带来的革命性优势: 高效的内存利用: 内存按需分配，一个块一个块地给，几乎没有内部浪费，使得在同样大小的显存下，可以服务的并发请求数量大大增加。 无内存碎片: 因为块的大小是固定的，所以内存管理变得非常简单高效，彻底解决了碎片问题。 实现复杂采样算法: PagedAttention使得KV Cache的复制和共享变得几乎没有成本（只需修改块表指针），从而可以高效地实现并行采样（Parallel Sampling）、Beam Search等需要复杂内存操作的高级解码算法。 3. vLLM的另一大创新：连续批处理 (Continuous Batching) 技术原理: vLLM采用了一种更智能、更灵活的请求调度策略。它将所有请求视为一个持续的流。一旦批处理中的任何一个请求完成了它的生成任务，调度器会立即将其从批次中移除，并从等待队列中无缝地插入一个新的请求来填补GPU的计算空位。 核心价值: 这种方法消除了传统静态批处理（Static Batching）中因等待批次中最慢请求而造成的GPU空闲时间，确保GPU始终处于高负载运行状态，从而极大地提升了系统的总吞吐量。 4. vLLM的核心架构 vLLM的架构设计精髓在于关注点分离（Separation of Concerns）和异步化（Asynchrony），它将一个复杂的推理任务拆解为多个独立的、可以并行或流水线工作的模块，从而最大化系统效率。
AsyncLLM: vLLM引擎的对外接口和通信层；关键优势: 异步化。它将网络I/O和CPU密集型的分词/反分词任务，与GPU的计算任务解耦，避免了互相等待。
接收请求: 负责与外部世界（如OpenAI兼容的API服务器）进行通信，接收用户发来的推理请求。
预处理: 对收到的文本提示（Prompt）进行分词（Tokenization），将其转换为模型可以理解的数字ID。
提交任务: 将预处理好的请求异步地提交给vLLM的内部处理队列。它提交后不会傻等结果，而是可以立刻去接收下一个新请求。
后处理: 当请求处理完成后，它会从结果队列中取回生成的token ID，并将其反分词（Detokenization），转换成人类可读的文本，最终返回给用户。
EngineCore： vLLM推理引擎的核心控制器；
主循环 (Busy Loop): EngineCore运行在一个持续的循环中，永不停歇。在每一轮循环（我们称之为一个“步”，step）中，它都会做三件事：</description></item></channel></rss>